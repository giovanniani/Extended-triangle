let
  var k := 10
in
  for var i := 1 to k do ! determinar que k se evalúa una sola vez
    k := 0 ; ! el valor del límite superior se establece arriba
    putint (i) ! debe imprimir 10 unos
  end
end